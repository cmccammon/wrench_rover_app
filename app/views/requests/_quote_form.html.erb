<%= form_for([@request, @request.quotes.new]) do |f|%>
  <% if @request.quotes.new.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request.quotes.new.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% @request.quotes.new.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p></p>
  <div class="field">
    <%= f.label :cost %><br>
    <%= f.text_field :cost %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
