<!DOCTYPE html>
<html>
  <head>
    <title>WrenchRoverApp</title>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render 'layouts/meganavbar' %>
  <main>
    <div class='container'>
      <div class='row'>
        <div class='m12'>
          <div class="container" style="margin-top:110px;">
            <% if notice %>
              <p class="alert alert-success alert-dismissible" role="alert"><%= notice %></p>
            <% end %>

            <% if alert %>
              <p class="alert alert-danger alert-dismissible" role="alert"><%= alert %></p>
            <% end %>
          <%= yield %>
        </div>
        </div>
      </div>
    </div>
    </main>
    <footer class="WRfooter">
      <div class="container">
        <div class="row">
          <div class="col-md-3 col-sm-3">
            <ul class="WRfooterLinks">
              <li><a href="#">Contact Us</a></li>
              <li><a href="#">Partners</a></li>
              <li><a href="#">FAQ</a></li>
              <% if service_center_signed_in? %>
              <li><%= link_to "Service Center Sign out", destroy_service_center_session_path, method: :delete, :class => 'navbar-link'  %></li>
              <% elsif user_signed_in? %>
              <li><%= link_to "Sign out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></li>
              <% end %>
            </ul>
          </div>
          <div class="col-md-3 col-sm-3">
            <p>Designed and built with all the love in the world by the WrenchRover Team. Maintained by the core dev team with the help of our contributors.</p>
            <p>Currently v1-alpha</p>
          </div>
          <div class="col-md-3 col-sm-3"><p>Copyright &copy; WrenchRover 2017</p>
          </div>
      </div>
    </div>
    </footer>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </body>
</html>
