<!DOCTYPE html>
<html lang="en">
  <head>
    <title>WrenchRoverApp</title>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>

  </head>

  <body>
    <%= render 'layouts/meganavbar' %>
  <main>
  <div class="container-overflow-wrap">
    <div class='container'>
      <div class='row'>
        <div class='m12'>
          <div class="container" style="margin-top:110px;">
            <% if notice %>
              <p class="alert alert-success alert-dismissible" role="alert"><%= notice %></p>
            <% end %>
            <% if alert %>
              <p class="alert alert-danger alert-dismissible" role="alert"><%= alert %></p>
            <% end %>

          <%= yield %>

        </div>
        </div>
      </div>
    </div>
  </div>
    </main>

    <footer class="WRfooter">
      <div class="container">
        <div class="row">
          <div class="col-md-12 col-sm-12 WRfooterLinks">
              <a href="#">Contact Us</a> | <a href="#">Partners</a> | <a href="#">FAQ</a>
              <% if service_center_signed_in? %> |
              <%= link_to "Service Center Sign out", destroy_service_center_session_path, method: :delete, :class => 'navbar-link'  %>
              <% elsif user_signed_in? %> |
              <%= link_to "Sign out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
              <% end %>
          </div>
        </div>
        <div class="row">
          <div class="col-md-12 col-sm-12">
            Designed and built with all the love in the world by the WrenchRover Team. Maintained by the core dev team with the help of our contributors.
          </div>
        </div>
        <div class="row">
          <div class="col-md-12 col-sm-12">Version: v1-alpha | Copyright &copy; WrenchRover 2017
          </div>
      </div>
    </div>
    </footer>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </body>
</html>
